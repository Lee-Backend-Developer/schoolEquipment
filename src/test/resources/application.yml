spring:
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true


  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:school
    username: sa
    password:

  flyway:
    locations: ""
  sql:
    init:
      mode: never


